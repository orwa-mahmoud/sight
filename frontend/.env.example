# Backend base URL. Leave unset to use same-origin relative requests:
# in dev the Vite server proxies /api + /webhooks to the backend (see
# vite.config.ts), and in Docker nginx reverse-proxies them. Set this only
# when the API is served from a different origin.
# VITE_API_URL=
